====================================================================
 ESP32-C3 SoftAP MQTT - Git Bash 完整部署命令
====================================================================

cd e:/Espidf/softap_mqtt

# 1. 初始化 Git 仓库
git init
git add .
git commit -m "Initial commit: ESP32-C3 SoftAP MQTT Config System with GitHub Actions"
git branch -M main

# ====================================================================
# 2. 创建 GitHub 仓库 - 两种方法选一个
# ====================================================================

# 方法 A: 如果您有 GitHub CLI (gh) - 推荐！
# 运行这个命令会自动创建仓库并推送
gh repo create esp32c3-softap-mqtt --public --push --source . --remote origin --description "ESP32-C3 SoftAP MQTT Config System"

# 方法 B: 手动创建仓库（如果没有 gh）
# 先去 https://github.com/new 创建仓库，然后运行：
# git remote add origin https://github.com/您的用户名/您的仓库名.git
# git push -u origin main

# ====================================================================
# 3. 等待 GitHub Actions 编译完成后下载 merged.bin
# ====================================================================
# 访问您的仓库: https://github.com/您的用户名/esp32c3-softap-mqtt
# 点击 Actions 标签
# 等待工作流变成绿色 ✔️ (约 3-5 分钟)
# 点进去 -> 页面底部 -> Artifacts -> 下载 esp32c3-firmware
# 解压 ZIP 包，里面就有 merged.bin!
